# CLAUDE.md

이 프로젝트는 부산 지역 특화 여행 계획 웹 서비스입니다.

## Claude 작업 원칙

- 대규모 리팩토링, 구조 변경, 불확실한 작업은 바로 수정하지 말고 먼저 계획을 제안합니다.
- UI 변경 전에는 변경 방향과 트레이드오프를 1~2줄로 요약한 뒤 수정합니다.
- 코드 수정 작업은 먼저 `AGENTS.md`의 작업 흐름과 커밋 규칙을 확인합니다.
- 기능 구현 시 서버 상태와 클라이언트 상태를 구분합니다.
  - 서버 데이터 요청/캐싱: TanStack Query
  - 전역 UI 상태/사용자 선택 상태: Zustand

- shadcn/ui 컴포넌트는 직접 수정하기보다 조합과 확장을 우선합니다.
- 임의의 라이브러리를 추가하기 전에는 이유와 대안을 먼저 설명합니다.
- 사용자가 명시하지 않은 기능, API, 도메인 로직을 임의로 만들지 않습니다.
- 부산 지역 데이터, 여행 일정 로직, 지도/위치 기반 기능은 확장 가능성을 고려해 분리합니다.

## 금지 사항

- `main` 브랜치에 직접 커밋하거나 push하지 않습니다.
- 사용자 승인 없이 대규모 폴더 구조 변경을 하지 않습니다.
- 테스트 또는 실행 확인 없이 완료했다고 말하지 않습니다.
- `--no-verify`로 훅을 우회하지 않습니다.
- UI만 맞추기 위해 도메인 로직을 컴포넌트 내부에 강하게 결합하지 않습니다.

## 기본 응답 방식

- 먼저 현재 작업 범위를 요약합니다.
- 필요한 경우 구현 전 계획을 짧게 제안합니다.
- 코드 변경 시 수정 파일과 이유를 함께 설명합니다.
- 불확실한 부분은 추측하지 말고 사용자에게 확인합니다.
